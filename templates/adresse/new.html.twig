{% extends 'base.html.twig' %}

{% block title %}Nouvelle Adresse{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/login.css') }}">
{% endblock %}

{% block body %}
    <div class="container py-4">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="card login-card shadow">
                    <div class="card-body p-4">
                        <nav aria-label="breadcrumb" class="mb-4">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Accueil</a></li>
                                <li class="breadcrumb-item"><a href="{{ path('app_adresse_index') }}">Adresses</a></li>
                                <li class="breadcrumb-item active">Nouvelle Adresse</li>
                            </ol>
                        </nav>

                        <h1 class="login-title mb-4">Nouvelle Adresse</h1>
                        
                        {{ include('adresse/_form.html.twig') }}
                        
                        <div class="mt-3">
                            <a href="{{ path('app_adresse_index') }}" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-2"></i>Retour à la liste
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
