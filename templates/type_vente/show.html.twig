{% extends 'base.html.twig' %}

{% block title %}TypeVente{% endblock %}

{% block body %}
    <h1>TypeVente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_vente.id }}</td>
            </tr>
            <tr>
                <th>NomTypeVente</th>
                <td>{{ type_vente.nomTypeVente }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ type_vente.createdAt ? type_vente.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ type_vente.updatedAt ? type_vente.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_vente_index') }}">back to list</a>

    <a href="{{ path('app_type_vente_edit', {'id': type_vente.id}) }}">edit</a>

    {{ include('type_vente/_delete_form.html.twig') }}
{% endblock %}
