<form method="post" action="{{ path('app_pochette_delete', {'id': pochette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pochette.id) }}">
    <button class="btn">Delete</button>
</form>
