{% extends 'base.html.twig' %}

{% block title %}Nouvelle École{% endblock %}

{% block body %}
    <div class="container py-4">
        <div class="card login-card shadow">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <div>
                        <nav aria-label="breadcrumb">
                            <ol class="breadcrumb mb-0">
                                <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Accueil</a></li>
                                <li class="breadcrumb-item"><a href="{{ path('app_ecole_index') }}">Écoles</a></li>
                                <li class="breadcrumb-item active">Nouvelle</li>
                            </ol>
                        </nav>
                        <h1 class="login-title mt-2">Nouvelle École</h1>
                    </div>
                </div>

                {{ include('ecole/_form.html.twig') }}

                <div class="mt-3">
                    <a href="{{ path('app_ecole_index') }}" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left me-2"></i>Retour à la liste
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
